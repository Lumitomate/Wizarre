name: Build and Deploy Web Game to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get Godot 4.7.2
        run: |
          wget -q https://github.com/godotengine/godot/releases/download/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip -O /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/
          chmod +x /tmp/Godot_v4.7.2-stable_linux.x86_64
          echo "GODOT=/tmp/Godot_v4.7.2-stable_linux.x86_64" >> $GITHUB_ENV

      - name: Get Export Templates
        run: |
          # curl --retry : le fichier fait ~1 Go, on se relance en cas de
          # coupure réseau pendant le téléchargement
          curl -sL --retry 5 --retry-delay 5 --retry-all-errors \
            https://github.com/godotengine/godot/releases/download/4.7.2-stable/Godot_v4.7.2-stable_export_templates.tpz -o /tmp/templates.tpz
          test -s /tmp/templates.tpz && test $(stat -c%s /tmp/templates.tpz) -gt 100000000
          mkdir -p ~/.local/share/godot/export_templates/4.7.2.stable/
          unzip -q /tmp/templates.tpz -d /tmp/templates/
          mv /tmp/templates/templates/* ~/.local/share/godot/export_templates/4.7.2.stable/

      - name: Import assets (génère .godot/imported pour l'export)
        run: $GODOT --headless --import
        shell: bash

      - name: Export Web Build
        run: |
          mkdir -p build/web
          $GODOT --headless --export-release "Web" build/web/index.html
        shell: bash

      - name: Setup Pages
        uses: actions/configure-pages@v5
        # Active Pages automatiquement au premier déploiement (source :
        # GitHub Actions), inutile de le faire à la main dans les Settings
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4