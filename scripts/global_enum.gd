class_name GlobalEnum

enum AttackType {
	FIREBALL,
	LIGHTRAY,
	FIRECOLUMN,
	ICEBALL
}

enum AttackTier {
	I,
	II,
	III,
}

enum SorcererColor {
	Red,
	Green,
	Blue,
	Yellow
}

enum AttackFamily {
	Red,
	Blue,
	Yellow,
	Green
}

enum State {
	IDLE, 
	RUN, 
	JUMP, 
	FALL,
	ATTACK
}

enum DoorType {
	TIME,
	FRIENDSHIP,
	NO_DAMAGE
}

enum Location {
	HOMEPAGE,
	LEVEL,
	SHOP
}
